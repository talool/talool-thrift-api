package com.talool.server;

import java.io.IOException;

import org.apache.thrift.TException;
import org.junit.Test;

public class ActivityUtilTest
{

	@Test
	public void testActivityUtil() throws TException, IOException
	{
		// Activity_t act = ActivityUtil.createPurchase(UUID.randomUUID(),
		// "We’ve been working hard making a faster, better repository experience and we’d love to share it with you.");

		// ThriftUtil.writeToDisk(ThriftUtil.serialize(act,
		// Constants.PROTOCOL_FACTORY), Constants.PROTOCOL_FACTORY,
		// "/tmp/act.thrift");

	}
}
